# Just adding this to ignore unneeded terraform files, 
# grabbed first example hit on the internet an adapted.

# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Local state lock file (as we won't collaborate together on this one, we would have the state e.g. on an S3 bucket)
.terraform.lock.hcl